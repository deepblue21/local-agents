{
  "name": "local-agents-web-console-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "playwright test --config=playwright.config.ts",
    "test:desktop": "playwright test --config=playwright.config.ts --project=desktop"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
